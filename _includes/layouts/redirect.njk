<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Redirecting...</title>
  {% metaRedirect targetUrl %}
  <style>
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
      padding: 1rem;
      text-align: center;
      background-color: #f9fafb;
    }
    .dark body {
      background-color: #1f2937;
      color: #f9fafb;
    }
    .container {
      max-width: 500px;
    }
    h1 {
      margin-bottom: 1rem;
    }
    p {
      margin-bottom: 2rem;
    }
    .spinner {
      border: 4px solid rgba(0, 0, 0, 0.1);
      width: 36px;
      height: 36px;
      border-radius: 50%;
      border-left-color: #4f46e5;
      animation: spin 1s linear infinite;
      margin: 0 auto 2rem;
    }
    .dark .spinner {
      border: 4px solid rgba(255, 255, 255, 0.1);
      border-left-color: #6366f1;
    }
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
    a {
      color: #4f46e5;
      text-decoration: none;
    }
    .dark a {
      color: #6366f1;
    }
  </style>
  <script>
    // Check if saved theme is dark
    const theme = localStorage.getItem('valuealign_theme') || localStorage.getItem('theme');
    if ((theme === 'dark') || (!theme && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
      document.documentElement.classList.add('dark');
    }
  </script>
</head>
<body>
  <div class="container">
    <div class="spinner"></div>
    <h1>Redirecting...</h1>
    <p>Our URL structure has been updated. You are being redirected to the new page.</p>
    <p>If you are not redirected automatically, <a href="{{ targetUrl }}">click here</a>.</p>
  </div>
</body>
</html>
